language: python

python:
  - 2.6
  - 2.7

install:
  - pip install -r requirements.txt
  - pip install -r test-requirements.txt

after_success:
  - coverage report -m
  - coveralls

notifications:
  email: false

script: coverage run --source django-pypuppetdb -m py.test --pep8